# ============================================================================
# Project Aegis — ServiceAccount (GC CronJob)
# ============================================================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: aegis-gc
  namespace: aegis
  labels:
    app.kubernetes.io/name: aegis-gc
    app.kubernetes.io/part-of: aegis
automountServiceAccountToken: false
